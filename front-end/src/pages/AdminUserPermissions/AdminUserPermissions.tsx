import PageContainer from '../../components/PageContainer';
import PageTitle from '../../components/PageTitle';

const AdminUserPermissions = () => {
  return (
    <PageContainer>
      <PageTitle title="Uprawnienia użytkowników" />
    </PageContainer>
  );
};

export default AdminUserPermissions;
